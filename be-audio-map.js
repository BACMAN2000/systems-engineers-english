// Auto-generated by tools/generate_be_audio.py — do NOT edit by hand.
// Maps cleaned-transcript SHA1 (16 chars) to the MP3 file path.
window.BE_AUDIO_MAP = {
};
window.BE_AUDIO_CLEAN = function(text){
  return (text||"")
    .replace(/(^|\s)[A-Z][A-Za-z ]{0,25}?:\s/g, ". ")
    .replace(/^\.\s*/, "")
    .replace(/\s+/g, " ")
    .trim();
};
window.BE_AUDIO_HASH = async function(text){
  const cleaned = window.BE_AUDIO_CLEAN(text);
  const enc = new TextEncoder().encode(cleaned);
  const buf = await crypto.subtle.digest("SHA-1", enc);
  const hex = Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,"0")).join("");
  return hex.slice(0, 16);
};